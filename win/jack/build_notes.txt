1.  VS Main Menu View->Property Manager
2.  When switching from OPTO, FIFO or COM change the property app->Debug|x64->proj->User Macros->$(FW_SHARE) directory
3.  Also change the CM_MEDIA_FTDI to CM_MEDIA_FIFO in ConView.cpp
4.  vc_redist.x64.exe must be added to install directory, VS2022 for release build
5.  Add "Install" at the end of the Post-Build step command line to create setup executable
6.  For the OPTO interface use FT_Prog to set SERIAL : O1XXXXXX,
    PORT A H/W : OPTO ISOLATE, DRIVER : D2XX DIRECT.6.  Use FT_Prog